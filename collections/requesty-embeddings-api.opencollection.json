{
  "opencollection": "1.0.0",
  "info": {
    "name": "Requesty Router API Keys Embeddings API",
    "version": "1.0"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Embeddings",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create embeddings",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://router.requesty.ai/v1/embeddings",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Generates vector embeddings for the supplied input text using a routed embedding model."
        }
      ]
    }
  ],
  "bundled": true
}